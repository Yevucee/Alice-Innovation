export default function CollectionsPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-16 md:px-6">
      <h1 className="text-2xl font-medium">Collections</h1>
      <p className="mt-4 text-sm leading-relaxed text-muted">
        Manual collections (South Tong Irrigation, FieldLens, client ideas, and similar) will appear here once the backend schema supports them.
        The navigation is in place so Alice can adopt collections without restructuring the interface.
      </p>
    </div>
  );
}
